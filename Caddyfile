{$DOMAIN} {
	# Real automatic HTTPS: Caddy provisions and renews a Let's Encrypt certificate for this
	# domain on its own, as long as DNS already points here and ports 80/443 are reachable.

	handle /api/* {
		reverse_proxy backend:4000
	}

	handle {
		reverse_proxy frontend:80
	}
}
