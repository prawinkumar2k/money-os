# Deferred models

Not yet implemented — schemas will be added when their feature module is built, per the build order in the project readme:

- Budget, BudgetItem
- Goal
- RecurringTransaction
- Subscription
- CreditCard
- Loan
- Investment, InvestmentTransaction
- SyncTransaction (per-transaction sync audit trail, if SyncJob's aggregate counters turn out to be insufficient)
- Notification
- Import
- UserRule (explicit categorization overrides, if Category.merchantRules turns out to be insufficient)
